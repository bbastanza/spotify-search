const addProductService = newProduct => {
    // TODO this is where the db interaction should be
    console.log("adding new product");
    console.log(newProduct);
};

module.exports = addProductService;
